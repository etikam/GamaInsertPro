
<nav class="navbar navbar-expand-lg navbar-dark px-5 py-3 py-lg-0">
            <a href="index.html" class="navbar-brand p-0">
                <h1 class="m-0"><i class="fa fa-user-tie me-2"></i>GamalInserPro</h1>
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse">
                <span class="fa fa-bars"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarCollapse">
                <div class="navbar-nav ms-auto py-0">
                    <a href="{{path('app_accueil')}}" class="nav-item nav-link active">Accueil</a>
                    {% if app.user %}  <!--Si l'utilisateur est authentifié,il peut voir les of -->
                        <a href="" class="nav-item nav-link">Offres</a>  
                        <a href="#" class="nav-item nav-link">Blog</a>
                    {% endif %}
                    
                    <a href="#" class="nav-item nav-link">Contact</a>
                </div>
                <button type="button" class="btn text-primary ms-3" data-bs-toggle="modal" data-bs-target="#searchModal"><i class="fa fa-search"></i></button>
                {% if app.user %}
                
                    <!-- <img   data-bs-toggle="offcanvas" data-bs-target="#offcanvasRight" aria-controls="offcanvasRight" src="{{asset('img/profile.jpg')}}" style="width:50px;height:50px"> -->
                   <i data-bs-toggle="offcanvas" data-bs-target="#offcanvasRight" aria-controls="offcanvasRight" class="fas fa-user fa-2x text-primary" style="margin-left:5px"></i>

                    <div class="offcanvas offcanvas-end" tabindex="-1" id="offcanvasRight" aria-labelledby="offcanvasRightLabel">
                    <div class="offcanvas-header">
                        <h5 class="offcanvas-title " id="offcanvasRightLabel">{{app.user.username}}</h5>
                       
                        <button type="button" class="btn-close" data-bs-dismiss="offcanvas" aria-label="Close"></button>
                    </div>
                    <div class="offcanvas-body">
                        <div class="row d-flex justify-content-center align-items-center " style="border-bottom:3px solid rgb(3, 109, 196); padding:2px">
                            <p class="col-5" style="margin:auto">{{app.user.getPrenom()}} {{app.user.getNom()}}  </p>
                           <img src="{{asset('img/profile.jpg')}}" alt="" class="col-5">
                        </div>
                        <div class="row d-flex justify-content-center align-items-center ">
                            <p class="col-12">app.user.get</p>
                        </div>
                        <a href="{{path('app_logout')}}" class="py-2 px-4 ms-3">Déconnexion</a>
                    </div>
                    </div>
                {% else %}                    
                    <a href="{{path('app_login')}}" class="btn btn-primary py-2 px-4 ms-3">Connexion</a>
                {% endif %}
              

                  


            </div>
        </nav>


         <!-- Full Screen Search Start -->
    <div class="modal fade" id="searchModal" tabindex="-1">
        <div class="modal-dialog modal-fullscreen">
            <div class="modal-content" style="background: rgba(9, 30, 62, .7);">
                <div class="modal-header border-0">
                    <button type="button" class="btn bg-white btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body d-flex align-items-center justify-content-center">
                    <div class="input-group" style="max-width: 600px;">
                        <input type="text" class="form-control bg-transparent border-primary p-3" placeholder="Type search keyword">
                        <button class="btn btn-primary px-4"><i class="bi bi-search"></i></button>
                    </div>
                </div>
            </div>
        </div>
    </div>