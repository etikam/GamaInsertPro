{% extends 'base.html.twig' %}

{% block title %}Postulation{% endblock %}

{% block body %}
    <h1>Postulation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ postulation.id }}</td>
            </tr>
            <tr>
                <th>DatePostulation</th>
                <td>{{ postulation.datePostulation ? postulation.datePostulation|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_postulation_index') }}">back to list</a>

    <a href="{{ path('app_postulation_edit', {'id': postulation.id}) }}">edit</a>

    {{ include('postulation/_delete_form.html.twig') }}
{% endblock %}
