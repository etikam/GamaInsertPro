{% extends 'base.html.twig' %}

{% block title %}TypeOffre{% endblock %}

{% block body %}
    <h1>TypeOffre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_offre.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ type_offre.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_offre_index') }}">back to list</a>

    <a href="{{ path('app_type_offre_edit', {'id': type_offre.id}) }}">edit</a>

    {{ include('type_offre/_delete_form.html.twig') }}
{% endblock %}
