<form method="post" action="{{ path('app_faculte_delete', {'id': faculte.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette faculté?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ faculte.id) }}">
    <button class="btn">Delete</button>
</form>
