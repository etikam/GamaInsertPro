{% extends 'base.html.twig' %}

{% block title %}Concentration{% endblock %}

{% block body %}
    <h1>Concentration</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ concentration.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ concentration.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_concentration_index') }}">back to list</a>

    <a href="{{ path('app_concentration_edit', {'id': concentration.id}) }}">edit</a>

    {{ include('concentration/_delete_form.html.twig') }}
{% endblock %}
